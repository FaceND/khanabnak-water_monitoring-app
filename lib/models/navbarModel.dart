// Module
import 'package:flutter/material.dart';

class Navigate {
  String title;
  String route;
  IconData icon;

  Navigate({
    required this.title,
    required this.route,
    required this.icon,
  });
}
