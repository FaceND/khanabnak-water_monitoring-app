// Module
import 'package:flutter/material.dart';

class Navigate {
  String route;
  String title;
  IconData icon;

  Navigate({
    required this.route,
    required this.title,
    required this.icon,
  });
}
